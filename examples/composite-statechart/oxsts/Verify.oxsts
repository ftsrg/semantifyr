type VerifyMissionEnvironment {
    feature statechart := CompositeStatechart()

    init {
        inline statechart.init()
    }

    tran {
        inline statechart.control.havoc()
        inline statechart.execute()
    }

    prop {
        statechart.B.Operation.isActive
    }
}

// compile Verify.oxsts -env VerifyMissionEnvironment -o verify.xsts
