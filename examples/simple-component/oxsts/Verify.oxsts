type VerifyMissionEnvironment {
    feature component := SimpleComponent()

    init {
        inline component.init()
    }

    tran {
        havoc (statechart.control)
        inline component.execute()
    }

    prop {
        component.client.Operation.isActive
    }
}

// compile Verify.oxsts -env VerifyMissionEnvironment -o verify.xsts
