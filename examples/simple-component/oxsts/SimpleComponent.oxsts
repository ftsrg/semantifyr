type SimpleComponent : CascadeComponent {
	ports += control : ConjugatedController // requires

	components += server : Server
	components += client : Client

	control = server.control

    channels += Channel {
        from := server.connection
        to := client.connection
    }
}
