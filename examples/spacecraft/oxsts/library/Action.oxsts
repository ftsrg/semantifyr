abstract type Action {

}

type RaiseEventAction : Action {
    reference event : Event

    tran {
        inline event.set()
    }
}

type SetTimeoutAction : Action {
    reference timeout : Timeout
    var amount : Integer
    var unit : TimeUnit

    tran {
        inline timeout.set(amount, unit)
    }
}
