type Mission : SynchronousComponent {
	ports += control : StationControl_Conjugated // requires

	components += satellite : Spacecraft
	components += station : GroundStation

	station.control = control

	channels += Channel {
		from := satellite.connection
		to := station.connection
	}
}

// execution environment modeling the various input events?


var mission = Mission()
mission.execute()
