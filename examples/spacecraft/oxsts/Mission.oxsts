type Mission : CascadeComponent {
	feature satellite :> components : Spacecraft
	feature station :> components : GroundStation

	feature control :> ports : StatationControlPort

	feature :> channels : Channel {
		from := satellite.connection
		to := station.connection
	}
}
